package com.blip.app.data.ble

import android.annotation.SuppressLint
import android.bluetooth.*
import android.bluetooth.le.*
import android.content.Context
import android.os.Build
import android.os.ParcelUuid
import com.blip.app.data.model.BlipPacket
import com.blip.app.data.model.BlipUser
import com.blip.app.data.model.ConnectionState
import com.blip.app.data.model.PacketType
import com.blip.app.data.model.deviceTagFrom
import dagger.hilt.android.qualifiers.ApplicationContext
import kotlinx.coroutines.flow.MutableStateFlow
import kotlinx.coroutines.flow.StateFlow
import java.util.UUID
import javax.inject.Inject
import javax.inject.Singleton

@Singleton
@SuppressLint("MissingPermission")
class BleManager @Inject constructor(
    @ApplicationContext private val context: Context
) {
    companion object {
        val BLIP_SERVICE_UUID: UUID = UUID.fromString("12345678-0000-1000-8000-00805f9b34fb")
        val BLIP_CHAR_UUID: UUID    = UUID.fromString("12345679-0000-1000-8000-00805f9b34fb")
        val BLIP_DESC_UUID: UUID    = UUID.fromString("00002902-0000-1000-8000-00805F9B34FB")
        const val MAX_PACKET_SIZE   = 512
    }

    private val bluetoothManager = context.getSystemService(BluetoothManager::class.java)
    private val bluetoothAdapter: BluetoothAdapter? get() = bluetoothManager?.adapter
    private val bleScanner: BluetoothLeScanner? get() = bluetoothAdapter?.bluetoothLeScanner
    private val bleAdvertiser: BluetoothLeAdvertiser? get() = bluetoothAdapter?.bluetoothLeAdvertiser

    // Devices that connected TO us as a GATT server (they are clients)
    private val connectedClients = mutableMapOf<String, BluetoothDevice>()

    // Our outbound GATT connections (we connected to them)
    private val gattClients = mutableMapOf<String, BluetoothGatt>()

    // Per-device write queues to avoid concurrent writes
    private val writeQueues = mutableMapOf<String, ArrayDeque<ByteArray>>()
    private val writeBusy  = mutableMapOf<String, Boolean>()

    // ── Canonical peer store — guarded by its own lock to survive rapid BLE callbacks ──
    private val peersLock = Any()
    private val peersMap  = LinkedHashMap<String, BlipUser>() // key = deviceAddress
    private val _nearbyUsers = MutableStateFlow<List<BlipUser>>(emptyList())
    val nearbyUsers: StateFlow<List<BlipUser>> = _nearbyUsers

    private val _incomingPackets = MutableStateFlow<BlipPacket?>(null)
    val incomingPackets: StateFlow<BlipPacket?> = _incomingPackets

    private val _isAdvertising = MutableStateFlow(false)
    val isAdvertising: StateFlow<Boolean> = _isAdvertising

    private val _isScanning = MutableStateFlow(false)
    val isScanning: StateFlow<Boolean> = _isScanning

    private var gattServer: BluetoothGattServer? = null
    private var localProfile: BlipUser? = null

    // ─── Advertising ─────────────────────────────────────────────────────────

    fun startAdvertising(user: BlipUser) {
        localProfile = user
        val settings = AdvertiseSettings.Builder()
            .setAdvertiseMode(AdvertiseSettings.ADVERTISE_MODE_LOW_LATENCY)
            .setConnectable(true)
            .setTimeout(0)
            .setTxPowerLevel(AdvertiseSettings.ADVERTISE_TX_POWER_HIGH)
            .build()

        val nameBytes = user.name.take(10).toByteArray()
        val serviceData = ByteArray(1 + nameBytes.size).also {
            it[0] = user.avatarColor.toByte()
            nameBytes.copyInto(it, 1)
        }

        val data = AdvertiseData.Builder()
            .setIncludeDeviceName(false)
            .setIncludeTxPowerLevel(false)
            .addServiceUuid(ParcelUuid(BLIP_SERVICE_UUID))
            .addServiceData(ParcelUuid(BLIP_SERVICE_UUID), serviceData)
            .build()

        bleAdvertiser?.startAdvertising(settings, data, advertiseCallback)
        startGattServer()
    }

    fun stopAdvertising() {
        bleAdvertiser?.stopAdvertising(advertiseCallback)
        gattServer?.close()
        _isAdvertising.value = false
    }

    private val advertiseCallback = object : AdvertiseCallback() {
        override fun onStartSuccess(settingsInEffect: AdvertiseSettings?) { _isAdvertising.value = true }
        override fun onStartFailure(errorCode: Int) { _isAdvertising.value = false }
    }

    // ─── GATT Server (receive incoming connections) ───────────────────────────

    private fun startGattServer() {
        val service = BluetoothGattService(BLIP_SERVICE_UUID, BluetoothGattService.SERVICE_TYPE_PRIMARY)
        val characteristic = BluetoothGattCharacteristic(
            BLIP_CHAR_UUID,
            BluetoothGattCharacteristic.PROPERTY_READ or
                    BluetoothGattCharacteristic.PROPERTY_WRITE or
                    BluetoothGattCharacteristic.PROPERTY_NOTIFY,
            BluetoothGattCharacteristic.PERMISSION_READ or
                    BluetoothGattCharacteristic.PERMISSION_WRITE
        )
        val descriptor = BluetoothGattDescriptor(
            BLIP_DESC_UUID,
            BluetoothGattDescriptor.PERMISSION_READ or BluetoothGattDescriptor.PERMISSION_WRITE
        )
        characteristic.addDescriptor(descriptor)
        service.addCharacteristic(characteristic)

        gattServer = bluetoothManager?.openGattServer(context, gattServerCallback)
        gattServer?.addService(service)
    }

    private val gattServerCallback = object : BluetoothGattServerCallback() {
        override fun onConnectionStateChange(device: BluetoothDevice, status: Int, newState: Int) {
            if (newState == BluetoothProfile.STATE_CONNECTED) {
                connectedClients[device.address] = device
            } else {
                connectedClients.remove(device.address)
                removeUserByAddress(device.address)
            }
        }

        override fun onCharacteristicWriteRequest(
            device: BluetoothDevice, requestId: Int,
            characteristic: BluetoothGattCharacteristic,
            preparedWrite: Boolean, responseNeeded: Boolean,
            offset: Int, value: ByteArray
        ) {
            if (characteristic.uuid == BLIP_CHAR_UUID) {
                parseIncomingPacket(value, device.address)
                if (responseNeeded) {
                    gattServer?.sendResponse(device, requestId, BluetoothGatt.GATT_SUCCESS, 0, null)
                }
            }
        }

        override fun onDescriptorWriteRequest(
            device: BluetoothDevice, requestId: Int,
            descriptor: BluetoothGattDescriptor,
            preparedWrite: Boolean, responseNeeded: Boolean,
            offset: Int, value: ByteArray
        ) {
            if (responseNeeded) {
                gattServer?.sendResponse(device, requestId, BluetoothGatt.GATT_SUCCESS, 0, null)
            }
        }
    }

    // ─── GATT Client (outbound connections) ──────────────────────────────────

    fun connectToDevice(device: BluetoothDevice) {
        if (gattClients.containsKey(device.address)) return
        updateUserConnectionState(device.address, ConnectionState.CONNECTING)
        device.connectGatt(context, false, buildGattClientCallback(device.address), BluetoothDevice.TRANSPORT_LE)
    }

    private fun buildGattClientCallback(address: String) = object : BluetoothGattCallback() {
        override fun onConnectionStateChange(gatt: BluetoothGatt, status: Int, newState: Int) {
            if (newState == BluetoothProfile.STATE_CONNECTED) {
                gattClients[address] = gatt
                updateUserConnectionState(address, ConnectionState.CONNECTED)
                gatt.discoverServices()
            } else {
                gattClients.remove(address)
                writeQueues.remove(address)
                writeBusy.remove(address)
                updateUserConnectionState(address, ConnectionState.DISCOVERED)
            }
        }

        override fun onServicesDiscovered(gatt: BluetoothGatt, status: Int) {
            // Services ready — caller can now send packets
        }

        override fun onCharacteristicWrite(
            gatt: BluetoothGatt,
            characteristic: BluetoothGattCharacteristic,
            status: Int
        ) {
            // Dequeue next pending write for this device
            val addr = gatt.device.address
            writeBusy[addr] = false
            drainQueue(addr)
        }

        override fun onCharacteristicChanged(
            gatt: BluetoothGatt,
            characteristic: BluetoothGattCharacteristic
        ) {
            @Suppress("DEPRECATION")
            characteristic.value?.let { parseIncomingPacket(it, gatt.device.address) }
        }

        @Suppress("OVERRIDE_DEPRECATION")
        override fun onCharacteristicChanged(
            gatt: BluetoothGatt,
            characteristic: BluetoothGattCharacteristic,
            value: ByteArray
        ) {
            parseIncomingPacket(value, gatt.device.address)
        }
    }

    // ─── Scanning ─────────────────────────────────────────────────────────────

    fun startScanning() {
        val filter = ScanFilter.Builder()
            .setServiceUuid(ParcelUuid(BLIP_SERVICE_UUID))
            .build()
        val settings = ScanSettings.Builder()
            .setScanMode(ScanSettings.SCAN_MODE_LOW_LATENCY)
            // FIRST_MATCH fires once when a device is first seen, MATCH_LOST fires when it goes away.
            // This prevents the OS flooding us with a callback every 150ms per device.
            .setCallbackType(
                ScanSettings.CALLBACK_TYPE_FIRST_MATCH or ScanSettings.CALLBACK_TYPE_MATCH_LOST
            )
            .setMatchMode(ScanSettings.MATCH_MODE_AGGRESSIVE)
            .setNumOfMatches(ScanSettings.MATCH_NUM_ONE_ADVERTISEMENT)
            .build()
        bleScanner?.startScan(listOf(filter), settings, scanCallback)
        _isScanning.value = true
    }

    fun stopScanning() {
        bleScanner?.stopScan(scanCallback)
        _isScanning.value = false
    }

    private val scanCallback = object : ScanCallback() {
        override fun onScanResult(callbackType: Int, result: ScanResult) {
            if (callbackType == ScanSettings.CALLBACK_TYPE_MATCH_LOST) {
                removeUserByAddress(result.device.address)
            } else {
                processScanResult(result)
            }
        }
        override fun onBatchScanResults(results: MutableList<ScanResult>) {
            results.forEach { processScanResult(it) }
        }
    }

    private fun processScanResult(result: ScanResult) {
        val serviceData = result.scanRecord?.getServiceData(ParcelUuid(BLIP_SERVICE_UUID)) ?: return
        if (serviceData.isEmpty()) return
        val avatarColor = serviceData[0].toInt()
        val name = if (serviceData.size > 1) String(serviceData.copyOfRange(1, serviceData.size)) else "Unknown"
        val addr = result.device.address

        // Synchronised so rapid back-to-back BLE callbacks can't race and add duplicates
        val needsConnect: Boolean
        synchronized(peersLock) {
            val existing = peersMap[addr]
            peersMap[addr] = BlipUser(
                id              = existing?.id ?: addr,
                name            = name,
                avatarColor     = avatarColor,
                deviceAddress   = addr,
                rssi            = result.rssi,
                lastSeen        = System.currentTimeMillis(),
                connectionState = existing?.connectionState ?: ConnectionState.DISCOVERED,
                deviceTag       = deviceTagFrom(addr)
            )
            needsConnect = !gattClients.containsKey(addr)
            _nearbyUsers.value = peersMap.values.toList()
        }

        if (needsConnect) connectToDevice(result.device)
    }

    // ─── Send Packet ──────────────────────────────────────────────────────────

    fun sendPacket(targetAddress: String, packet: BlipPacket) {
        val data = serializePacket(packet)
        val gatt = gattClients[targetAddress]
        if (gatt != null) {
            enqueueWrite(targetAddress, gatt, data)
        } else {
            // Fallback: notify via server side if they connected to us
            val device = connectedClients[targetAddress] ?: return
            notifyDevice(device, data)
        }
    }

    fun broadcastPacket(packet: BlipPacket) {
        val data = serializePacket(packet)
        // Send via all outbound GATT connections
        gattClients.forEach { (addr, gatt) -> enqueueWrite(addr, gatt, data) }
        // Also notify inbound clients
        connectedClients.values.forEach { device -> notifyDevice(device, data) }
    }

    // ─── Write Queue (prevents concurrent BLE writes) ─────────────────────────

    private fun enqueueWrite(address: String, gatt: BluetoothGatt, data: ByteArray) {
        val queue = writeQueues.getOrPut(address) { ArrayDeque() }
        queue.addLast(data)
        if (writeBusy[address] != true) drainQueue(address)
    }

    private fun drainQueue(address: String) {
        val gatt = gattClients[address] ?: return
        val queue = writeQueues[address] ?: return
        if (queue.isEmpty()) return
        val data = queue.removeFirst()
        val service = gatt.getService(BLIP_SERVICE_UUID) ?: return
        val char = service.getCharacteristic(BLIP_CHAR_UUID) ?: return
        writeBusy[address] = true
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
            gatt.writeCharacteristic(char, data, BluetoothGattCharacteristic.WRITE_TYPE_DEFAULT)
        } else {
            @Suppress("DEPRECATION")
            char.value = data
            @Suppress("DEPRECATION")
            gatt.writeCharacteristic(char)
        }
    }

    private fun notifyDevice(device: BluetoothDevice, data: ByteArray) {
        val characteristic = gattServer
            ?.getService(BLIP_SERVICE_UUID)
            ?.getCharacteristic(BLIP_CHAR_UUID) ?: return
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
            gattServer?.notifyCharacteristicChanged(device, characteristic, false, data)
        } else {
            @Suppress("DEPRECATION")
            characteristic.value = data
            @Suppress("DEPRECATION")
            gattServer?.notifyCharacteristicChanged(device, characteristic, false)
        }
    }

    // ─── Connection state helpers ─────────────────────────────────────────────

    fun updateUserConnectionState(deviceAddress: String, state: ConnectionState) {
        synchronized(peersLock) {
            val existing = peersMap[deviceAddress] ?: return
            peersMap[deviceAddress] = existing.copy(connectionState = state)
            _nearbyUsers.value = peersMap.values.toList()
        }
    }

    // ─── Packet Serialization ─────────────────────────────────────────────────

    fun serializePacket(packet: BlipPacket): ByteArray {
        val senderBytes    = packet.senderId.toByteArray()
        val recipientBytes = packet.recipientId.toByteArray()
        return byteArrayOf(packet.type.code) +
                senderBytes + byteArrayOf(0x00) +
                recipientBytes + byteArrayOf(0x00) +
                byteArrayOf(packet.ttl.toByte()) +
                longToBytes(packet.timestamp) +
                packet.payload
    }

    private fun parseIncomingPacket(data: ByteArray, senderAddress: String) {
        try {
            if (data.isEmpty()) return
            val type = PacketType.fromCode(data[0]) ?: return
            var idx = 1
            val senderEnd = data.drop(idx).indexOfFirst { it == 0x00.toByte() }.let { if (it == -1) return else it + idx }
            val senderId = String(data.copyOfRange(idx, senderEnd))
            idx = senderEnd + 1
            val recipientEnd = data.drop(idx).indexOfFirst { it == 0x00.toByte() }.let { if (it == -1) return else it + idx }
            val recipientId = String(data.copyOfRange(idx, recipientEnd))
            idx = recipientEnd + 1
            if (idx >= data.size) return
            val ttl = data[idx++].toInt()
            if (idx + 8 > data.size) return
            val timestamp = bytesToLong(data.copyOfRange(idx, idx + 8))
            idx += 8
            val payload = if (idx <= data.size) data.copyOfRange(idx, data.size) else ByteArray(0)
            _incomingPackets.value = BlipPacket(type, senderId, recipientId, payload, timestamp, ttl)
        } catch (_: Exception) { }
    }

    private fun removeUserByAddress(address: String) {
        synchronized(peersLock) {
            peersMap.remove(address)
            _nearbyUsers.value = peersMap.values.toList()
        }
    }

    private fun longToBytes(v: Long): ByteArray =
        ByteArray(8) { i -> ((v shr ((7 - i) * 8)) and 0xFF).toByte() }

    private fun bytesToLong(b: ByteArray): Long =
        b.fold(0L) { acc, byte -> (acc shl 8) or (byte.toLong() and 0xFF) }
}
